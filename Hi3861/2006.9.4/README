# QST 鸿蒙小车：双路红外巡线与指定岔路转向

基于 Hi3861 与 STM32 电机控制板的低速巡线实验：沿白—黑—白胶带道路行驶，**第一个岔路左转，第二个岔路右转**，超声波检测到前方障碍时停车。

> 当前为实验代码，不是经过实车验证的完整自动驾驶方案。默认电机驱动关闭；只完成文件整理与 C 语法层面检查，尚未通过目标 SDK 编译、执行测试程序、烧录或跑完整条路线。

## 文档入口

- [完整工程代码（Markdown）](PROJECT_CODE.md)：所有源码、构建配置和测试代码，逐文件列出，可复制还原。
- [详细工程 README](17.0_TwoSensor_LineFollowing/README.md)：引脚、接线约定、调试流程、原因码及限制。
- [2026-09-04 学习日志](LEARNING_LOG_2026-09-04.md)：需求纠正、原理、完成内容和待验证项目。

## 场地与读数

| 项目 | 当前信息 |
| --- | --- |
| 两个底部探头中心间距 | 约 6.5 cm |
| 道路宽度 | 左白 2.5 cm＋黑 2.5 cm＋右白 2.5 cm，总宽约 7.5 cm |
| 黑胶带 | 用户反馈归一化检测值为 1 |
| 白胶带与地砖 | 用户反馈均为 0，现有读数无法区分 |
| 岔路 | 用户反馈左右同时为 1,1；并未证实该信号仅出现在岔路 |
| 路线顺序 | 相对小车前进方向，第一岔路左转、第二岔路右转 |

`black=1` 是软件判断结果，不等于 GPIO 原始电平必然为高。原代码的低电平为黑映射仍需对照硬件确认。

## 主要逻辑

1. 正常巡线：双 0 低速直行；左黑向左修正，右黑向右修正。
2. 双黑时先停轮，持续约 30 ms 后接受为路口候选。
3. 第一次候选向左转，第二次向右转。
4. 转向至少约 120 ms 后，尝试通过持续单黑再双白的序列重新跟线。它只是启发式判据，不证明选对分支。
5. 离开路口阶段抑制重复计数，连续非双黑约 300 ms 后重新允许计数。
6. 障碍、读数异常、转弯/退出路口超时或总测试时限到达，均锁定停车。
7. 起步即双黑，或两次转向后再次双黑，均锁停；不宣告已确认到达终点。

## 仓库目录

```text
.
├── README.md
├── PROJECT_CODE.md
├── LEARNING_LOG_2026-09-04.md
├── .gitignore
└── 17.0_TwoSensor_LineFollowing/
    ├── README.md
    ├── BUILD.gn
    ├── LineFollowing.c
    ├── line_config.h
    ├── line_control.h
    ├── line_control.c
    └── tests/
        └── test_line_control.c
```

Markdown 用于 GitHub 阅读，真正参与固件构建的是对应的 `.c`、`.h` 和 `BUILD.gn` 文件。本包只含 17.0，不覆盖旧 15.0 / 16.0 工程。

## 编译与试验

这是现有 OpenHarmony 源码树的应用组件，**不是包含全部 SDK 的独立固件仓库**。

1. 把 `17.0_TwoSensor_LineFollowing` 文件夹复制到现有 SDK 的 `applications/sample/wifi-iot/app/`。
2. 将该父目录 `BUILD.gn` 的 `features` 改为以下内容，保留其余必要配置，避免同时启动旧的电机控制示例：

```gn
features = [
  "17.0_TwoSensor_LineFollowing:TwoSensor_LineFollowing",
]
```

3. 在 SDK 源码根目录执行：

```bash
python build.py wifiiot
```

4. 编译成功后，按开发板原有流程烧录。保持 `line_config.h` 中 `LINE_MOTORS_ENABLED = 0`，先检查传感器读数。
5. 车轮架空确认方向后，才将其改为 `1`，重新编译烧录，进行有人看护的低速测试。摆放位置应越过起点横条，且尚未到第一岔路。

默认测试总时限为 10 秒；连续双白 4 秒、同侧修正 1 秒也可能停车。它们用于限制盲行，不是准确脱线检测器，可能在正常长直线/长弯触发。转弯参数须实测校准，不要直接当作全程参数。

控制逻辑测试源码可在有 GCC 的环境中运行；在组件目录执行：

```bash
gcc -std=c99 -Wall -Wextra -Werror -I. line_control.c tests/test_line_control.c -o /tmp/qst_line_control_test
/tmp/qst_line_control_test
```

交
