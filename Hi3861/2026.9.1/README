本仓库包含两个配套工程：

- `hi3861-edge-controller`：读取车头左、右两个红外反射传感器，执行边缘判断和避让状态机，并通过 UART 发送左右轮命令。
- `stm32-uart-motor-controller`：通过 USART2 接收 6 字节控制帧，进行有效性检查和通信超时保护，再输出左右电机 PWM。

## 系统结构

```text
左前/右前红外传感器
          ↓
       Hi3861
  边缘判断与避让状态机
          ↓ UART 115200 8N1
       STM32F103
  协议解析、超时保护、PWM
          ↓
      左右电机驱动
```

## 小车行为

```text
正常前进
├─ 左前检测到边缘：停止 → 后退 → 右转 → 继续前进
├─ 右前检测到边缘：停止 → 后退 → 左转 → 继续前进
└─ 两侧同时检测：停止 → 后退 → 交替选择左/右转 → 继续前进
```

转弯前先短暂后退，避免小车在桌边直接旋转。

## 通信协议

固定发送 6 个字节：

| 索引 | 字段 | 取值 |
| ---: | --- | --- |
| 0 | 帧头 | `0xFC` |
| 1 | 左轮方向 | `0` 正转，`1` 反转 |
| 2 | 左轮速度 | `0～150` |
| 3 | 右轮方向 | `0` 正转，`1` 反转 |
| 4 | 右轮速度 | `0～150` |
| 5 | 帧尾 | `0xFD` |

示例：

```text
前进：FC 00 3C 00 3C FD
停止：FC 00 00 00 00 FD
后退：FC 01 32 01 32 FD
左转：FC 01 32 00 32 FD
右转：FC 00 32 01 32 FD
```

## 接线

### 红外传感器到 Hi3861

```text
左前红外数字输出 → Hi3861 GPIO5
右前红外数字输出 → Hi3861 GPIO6
传感器 GND       → Hi3861 GND
```

### Hi3861 到 STM32

```text
Hi3861 GPIO11 / UART2_TX → STM32 PA3 / USART2_RX
Hi3861 GND               → STM32 GND
```

需要双向通信时可再连接：

```text
Hi3861 GPIO12 / UART2_RX ← STM32 PA2 / USART2_TX
```

注意：两块控制板必须共地，传感器数字输出不得超过 Hi3861 GPIO 可接受的电压。

## 编译和烧录顺序

1. 将 `hi3861-edge-controller` 放入 OpenHarmony `applications/sample/wifi-iot/app/` 下对应的应用目录。
2. 根据该目录 README 配置 `BUILD.gn`，运行 `python build.py wifiiot`。
3. 将生成的 `Hi3861_wifiiot_app_allinone.bin` 烧录到 Hi3861。
4. 将 `stm32-uart-motor-controller` 中的应用文件加入课程原有的 STM32F103 Keil 工程。
5. 确保课程工程已经包含系统时钟、延时、USART1 调试和 STM32 标准外设库。
6. 编译并通过 ST-Link 将 STM32 程序烧录到小车主板。
7. 断电接线，先架空车轮测试，再进行地面和桌面测试。

## 红外有效电平

Hi3861 工程默认：

```c
#define EDGE_ACTIVE_LEVEL WIFI_IOT_GPIO_VALUE1
```

如果传感器悬空时输出低电平，改成：

```c
#define EDGE_ACTIVE_LEVEL WIFI_IOT_GPIO_VALUE0
```

修改后重新编译并烧录 Hi3861。

## 安全说明

- 第一次测试必须架空车轮。
- 桌面测试时必须有人在桌边接住小车。
- 电机使用电池或电机驱动电源，不能由 GPIO 直接供电。
- 如果 300 ms 内没有收到有效 UART 命令，STM32 会自动停车。
- 当前 STM32 示例采用开环 PWM，适合先完成通信和避边验证；后续可将目标速度接入原课程的编码器 PID 
