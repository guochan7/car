# 2026-09-02 学习日志：Hi3861 蓝牙遥控无人车

## 今日学习目标

今天的主要目标是完成 Hi3861 蓝牙遥控无人车程序，让手机通过 JDY-16 向 Hi3861 发送控制字符，并由 Hi3861 按课程原协议把运动命令转发给 STM32。同时，我也复习了无人车的基础理论，包括差速运动、电机驱动、PWM、串口通信、通信协议和供电关系。

## 一、今天完成的工程内容

### 1. 理清整个控制链路

我首先重新梳理了无人车的信号传递过程：

```text
手机 -> JDY-16 -> Hi3861 -> STM32 -> 电机驱动 -> 左右电机
```

手机发送的是 `W/A/S/D/O` 等字符。JDY-16 把蓝牙数据转换成串口数据，Hi3861 负责解析这些字符，再把左右电机的方向和速度封装成六字节数据帧发给 STM32。STM32 最后根据数据控制 PWM 和电机方向。

这一步让我明确了各模块的职责：Hi3861 负责蓝牙通信和上层命令处理，STM32 负责底层电机控制，二者不能混为一谈。

### 2. 创建 Hi3861 蓝牙控制模块

我在 OpenHarmony 源码的以下位置创建了实验目录：

```text
applications/sample/wifi-iot/app/14.0_Bluetooth_control
```

目录中包含 `Control.c` 和 `BUILD.gn`。随后在应用总 `BUILD.gn` 中启用：

```text
14.0_Bluetooth_control:Control
```

程序使用两个串口：

- UART1：GPIO0/GPIO1，9600 bit/s，与 JDY-16 通信
- UART2：GPIO11/GPIO12，115200 bit/s，与 STM32 通信

### 3. 实现手机控制字符

我实现了以下控制字符：

| 字符 | 功能 |
| --- | --- |
| `W` | 前进 |
| `S` | 后退 |
| `A` | 左转 |
| `D` | 右转 |
| `O` | 停止 |
| `I` | 普通速度前进 |
| `K` | 较高速度前进 |

为了提高容错性，代码还能把小写字符转换为大写，因此手机发送 `w` 时也能按 `W` 处理。

### 4. 恢复课程原六字节协议

课程使用的 Hi3861 到 STM32 数据帧为：

```text
0xFC, 左轮方向, 左轮速度, 右轮方向, 右轮速度, 0xFD
```

方向值 `0` 表示正转，`1` 表示反转；速度范围为 `0~150`。我在代码中加入了速度绝对值处理和上限限制，避免发出不符合协议的数据。

今天也发现，之前电脑直接测试 STM32 时使用过 `@F`、`@S` 一类调试命令，但它们不是这次课程要求的 Hi3861 原协议。最终工程应以 `0xFC ... 0xFD` 六字节帧为准。

### 5. 解决编译问题

今天遇到了几类典型问题：

1. 实验文件夹名称和 `BUILD.gn` 中的名称不一致，导致构建系统找不到模块。
2. 头文件名称写成了 `wifiot_uart.h`，实际应为 `wifiiot_uart.h`。
3. `hi_sleep()` 缺少声明，需要加入 `hi_task.h`。
4. 头文件目录需要使用 `//base/iot_hardware/interfaces/kits/wifiiot_lite`。
5. `app_main.c` 的实际路径中是 `wifiiot_app`，不是 `wifiot_app`。
6. 根据课程双串口实验要求，把 `APP_INIT_EVENT_NUM` 从 `4` 调整为 `7`。

逐项修正后，终端最终出现：

```text
BUILD SUCCESS
ohos wifiot build success!
```

### 6. 完成固件烧录和启动检查

编译完成后，我烧录了：

```text
out/wifiot/Hi3861_wifiiot_app_allinone.bin
```

烧录并复位后，串口输出：

```text
Bluetooth car control ready. UART1=9600, UART2=115200.
```

这说明 Hi3861 程序已经启动，两个串口也完成了初始化。随后手机成功连接 JDY-16，并能在蓝牙调试器中发送 `W`。

目前可以确认 Hi3861 侧的编译、烧录、启动和蓝牙连接过程已经完成。整车电机动作还需要 STM32 侧烧录能够识别课程六字节帧的程序，并保证电池、电源开关、串口交叉连接和共地都正确。

## 二、今天复习的无人车理论知识

### 1. 两轮差速小车运动原理

两轮差速小车通过控制左右轮的速度和方向完成运动：

- 左右轮同速正转：前进
- 左右轮同速反转：后退
- 左轮慢、右轮快：向左转弯
- 左轮快、右轮慢：向右转弯
- 左右轮反向转动：原地转向
- 两轮速度为零：停止

因此，小车的转向不一定需要舵机，也可以依靠左右轮的速度差完成。

### 2. PWM 调速原理

PWM 通过快速改变高、低电平的占空比来调节电机获得的平均功率。占空比越高，电机通常转得越快；占空比越低，电机通常转得越慢。

程序中的速度数值并不是直接等于电机的实际转速，而是 STM32 进一步转换为 PWM 输出。实际速度还会受到电池电压、电机负载、地面摩擦和左右电机差异的影响。

### 3. H 桥电机驱动原理

微控制器的 GPIO 不能直接给电机提供足够电流，因此需要电机驱动芯片。H 桥通过改变电流流过电机的方向，实现正转、反转和停止；PWM 则用于控制输出功率。

### 4. 串口通信基础

本实验使用异步串口，参数均为 8N1：

- 8 个数据位
- 无校验位
- 1 个停止位

通信双方必须使用相同的波特率和数据格式。接线时 TX 接对方 RX、RX 接对方 TX，同时必须共地，否则即使程序正确也可能无法通信。

### 5. 通信协议的重要性

串口只负责传输字节，接收方还需要知道这些字节分别代表什么。帧头和帧尾可以帮助接收方定位一条完整命令，固定字段则让方向与速度具有明确含义。

如果 Hi3861 使用六字节协议，而 STM32 只认识 `@F` 这样的文本协议，那么两边虽然波特率一致，也不会产生正确动作。因此，通信双方的协议必须完全匹配。

### 6. 供电与信号的区别

USB 连接、串口通信和电机供电是三个容易混淆的概念：

- USB 可以用于烧录、查看日志或给逻辑电路供电。
- 手机与 JDY-16 的蓝牙连接只负责传输控制指令。
- 电机需要更大的电流，通常必须由小车电池和电机电源回路供电。

所以，看到程序运行和蓝牙连接成功，并不代表电机已经获得足够的供电。

## 三、今天的收获

今天最大的收获不是只把程序编译成功，而是把“蓝牙命令、Hi3861 双串口、STM32 协议、电机驱动和供电”看成了一条完整链路。排查问题时不能只看某一个界面显示“Connected”，而要逐段验证数据是否真正传到了下一层。

我也进一步理解了：工程中的文件名、头文件、构建路径和通信协议必须保持一致。一个字符或一个目录名称的差异，就可能让整个工程无法编译；一套不匹配的协议，则会让程序看起来都在运行，但小车没有任何动作。
