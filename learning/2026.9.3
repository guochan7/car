# 2026-09-03 学习日志：鸿蒙小车超声波与红外综合避障

## 今日目标

- 阅读 QST 鸿蒙小车原理图，确认传感器和控制接口。
- 编写一个超声波自动避障工程。
- 在超声波避障基础上加入底部双路红外黑胶带检测。
- 整理适合上传 GitHub 的 README 和学习记录。

## 今日完成内容

### 1. 超声波避障工程

建立了 `15.0_Ultrasonic_Avoidance` 工程，实现：

- 小车自动直行。
- 超声波测量前方距离。
- 距离小于或等于 25 cm 时停车并后退。
- 舵机分别观察左侧和右侧。
- 比较左右距离，向空间较大的一侧转向。
- 超声波无有效回波时安全停车。

### 2. 超声波与红外综合避障工程

建立了 `16.0_Ultrasonic_Infrared_Avoidance` 工程，实现：

- 保留超声波避障功能。
- GPIO13、GPIO14 读取底部左右 TCRT5000 红外对管。
- 检测到黑胶带时立即停车。
- 左传感器检测到黑带时向右转。
- 右传感器检测到黑带时向左转。
- 两侧同时检测到黑带时交替选择转向方向。
- 转弯后自动向前移动一小段，防止在胶带上重复触发。

## 原理图引脚整理

| 功能 | 引脚 | 备注 |
| --- | --- | --- |
| 舵机 | GPIO2 | 软件生成舵机脉冲 |
| 超声波 TRIG | GPIO7 | 输出 10 μs 触发脉冲 |
| 超声波 ECHO | GPIO8 | 输入并测量高电平时间 |
| STM32 UART2 TX | GPIO11 | Hi3861 向 STM32 发送电机命令 |
| STM32 UART2 RX | GPIO12 | STM32 向 Hi3861 返回数据 |
| 左红外对管 | GPIO13 | 低电平表示检测到黑色 |
| 右红外对管 | GPIO14 | 低电平表示检测到黑色 |

## 今天学到的关键知识

### 1. 超声波距离计算

超声波从发射到接收走了一个往返距离，因此需要除以 2：

```c
distanceCm = ((float)echoTimeUs * 0.034f) / 2.0f;
```

其中 `0.034 cm/μs` 是近似声速。

### 2. 不能无限等待 ECHO

如果传感器没有收到回波，程序可能一直卡在等待循环中。解决办法是使用 `hi_get_us()` 计时并设置超时：

```c
if ((uint32_t)(hi_get_us() - waitStart) > ULTRASONIC_TIMEOUT_US) {
    return -1.0f;
}
```

### 3. 多次采样可以降低误触发

超声波使用多个有效值取平均；红外对管使用 5 次采样中的多数结果。这样可以过滤短暂噪声和电平抖动。

### 4. 红外对管的有效电平必须由实际工程确认

只看比较器原理图容易对输出极性产生误判。开发板自带 `TCRT.c` 示例明确说明：

```c
if (id_status == WIFI_IOT_GPIO_VALUE0) {
    printf("black\r\n");
}
```

因此本项目最终使用低电平表示黑色胶带。

### 5. 传感器任务需要优先级

在综合工程中，地面的黑胶带通常代表边界，必须比普通墙面避障更优先。因此主循环先读取红外对管，未检测到黑带时才执行超声波测距。

## 遇到的问题与解决方法

### 问题 1：`hi_sleep()` 编译时报隐式声明

错误信息：

```text
implicit declaration of function 'hi_sleep'
```

原因是源文件缺少对应头文件。解决方法：

```c
#include "hi_task.h"
```

补充后，`15.0_Ultrasonic_Avoidance` 工程成功编译并显示 `BUILD SUCCESS`。

### 问题 2：无法只凭原理图确定黑白电平

最初根据 LM393 电路推测电平，随后与开发板自带 `2.0_TCRT_Timer/TCRT.c` 对照，确认 GPIO13、GPIO14 为低电平时检测到黑色，及时修正了综合工程。

### 问题 3：转弯后可能反复检测到同一条胶带

如果转向结束时传感器仍在胶带上，主循环会再次触发。解决方法是在转弯后向前行驶一小段：

```c
CarForward();
hi_sleep(TAPE_CLEAR_TIME_MS);
```

## 当前状态

| 项目 | 状态 |
| --- | --- |
| `15.0_Ultrasonic_Avoidance` | 已成功编译 |
| `16.0_Ultrasonic_Infrared_Avoidance` | 代码、BUILD.gn 和父工程入口已完成，等待重新连接虚拟机后执行最终编译 |
| GitHub 文档 | 两个 README 和本学习日志已完成 |

## 下一步计划

1. 重新连接虚拟机并编译综合工程。
2. 将小车轮子架空，验证左右轮方向。
3. 使用白纸和黑胶带校准左右红外电位器。
4. 在空旷场地调整 `TAPE_TURN_TIME_MS` 和 `TAPE_CLEAR_TIME_MS`。
5. 在墙面与黑胶带同时存在的环境进行综合测试。
6. 将测试视频、场地照片和最终参数补充到 GitHub。

## 今日总结

今天完成了从原理图引脚确认、传感器读取、电机控制、异常保护到多传感器决策的完整流程。最重要的经验是：嵌入式开发不能只依赖理论推测，必须结合板载示例、编译结果和实车电平进行验证。
