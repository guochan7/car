# 实训工作日志

日期：2026年8月31日  
项目：OpenHarmony智能小车开发  
开发平台：Hi3861、OpenHarmony、VS Code

## 一、今日完成内容

1. 完成虚拟机与Windows主机的网络连接配置，使用VS Code远程连接虚拟机中的OpenHarmony工程。

2. 学习并调试红外对管收发功能：

   - 初始化红外发射和接收相关GPIO。
   - 控制红外发射端输出信号。
   - 读取红外接收端的电平变化。
   - 通过串口打印接收结果，判断红外信号是否正常。
   - 进一步了解红外传感器在小车寻线、避障等场景中的应用。

3. 完成AP3216C环境光传感器实验：

   - 初始化AP3216C传感器。
   - 周期性读取环境光ALS数据。
   - 设置黑暗和明亮两个判断阈值。
   - 环境较暗时打开GPIO6车灯，环境变亮后自动关闭车灯。
   - 使用两个不同阈值避免车灯在临界亮度下频繁闪烁。

4. 编写第一阶段综合实验代码：

   - 任务1控制舵机左右转动，并配合超声波传感器测量左右距离。
   - 任务2在启动后的前15秒读取红外对管数据，进行寻线状态判断。
   - 15秒后，任务2切换为UART蓝牙通信。
   - 任务3负责读取并打印消息队列中的信息。
   - 使用信号量实现任务1和任务2交替运行。
   - 使用消息队列完成多个任务之间的数据传递。

5. 修改工程中的`BUILD.gn`文件，将综合实验模块加入OpenHarmony编译系统。

## 二、遇到的问题及解决方法

1. 编译时出现头文件找不到的问题。

   解决方法：根据当前OpenHarmony版本，将GPIO头文件改为`wifiiot_gpio.h`和`wifiiot_gpio_ex.h`，并检查`BUILD.gn`中的头文件目录。

2. 程序中出现变量或阈值未定义的问题。

   解决方法：在文件开头统一定义黑暗开灯阈值和明亮关灯阈值，保证变量在使用前已经声明。

3. 创建线程时出现函数类型不匹配的问题。

   解决方法：在线程创建时将任务函数转换为`osThreadFunc_t`类型。

4. 多个任务同时运行时容易出现执行顺序混乱。

   解决方法：使用两个信号量控制任务1和任务2交替运行，并使用消息队列将运行结果交给任务3统一打印。

## 三、今日成果

完成了红外对管收发、AP3216C光照采集、自动车灯控制以及多任务综合联动程序的编写，对Hi3861的GPIO、UART、传感器驱动、线程、信号量和消息队列有了进一步理解。
