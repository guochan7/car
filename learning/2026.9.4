# 2026-09-04 学习日志：从避黑到沿黑线巡线

## 今天确定的任务

使用白—黑—白胶带道路。按用户最新纠正，以小车行进方向为基准，**第一个岔路左拐、第二个岔路右拐**。底部左右探头中心间距约 6.5 cm，中央黑带宽 **2.5 cm**；原先记录的 4 cm 作废。

用户随后补充白色部分与黑色部分一样宽，按左右白色部分各 2.5 cm 记录，总带宽约 7.5 cm。

## 关键认识

1. 避黑与巡线的修正方向相反。避黑时远离黑色；本次巡线中，左探头压黑要向左修正，右探头压黑要向右修正。
2. 在上述几何关系下，居中时两个探头各距黑带边缘约 2 cm、距白带外边缘约 0.5 cm。外侧探头可能先进入地砖，内侧探头还未接触黑线；不能把未检测到黑线等同于车身居中。实际检测光斑也会缩小边缘余量。带宽纠正后，原先的 1.25 cm 推算作废。
3. 双白不一定是居中，也可能是脱线后看到了地砖。双黑不一定是终点，也可能是岔路或斜压黑带。
4. 只有两路开关量，不能可靠恢复连续横向位置，也不能单靠固定转弯时间保证走对分叉。
5. 为保持在路线内，本版本遇到超声波障碍时停车，不执行原避障工程的倒车绕行。

## 今天生成的内容

- 新增独立的 17.0 巡线测试工程：硬件接口、控制状态机、参数文件、构建配置、README 和测试源码。
- 默认不驱动车轮，先核对黑白与左右读数。
- 加入起点及超出计划次数的双黑锁停、测距异常锁停、GPIO 错误处理、测试总时限及连续状态时限；计划内的双黑改为先停轮确认再转向。
- 未修改原 15.0 / 16.0 的程序逻辑；未上传 GitHub。
- 根据用户反馈“岔路口两路同时为 1”，新增实验路线层：双黑停车确认后，第一路口左转、第二路口右转；转弯后等待单黑再双白的捕线候选序列，并抑制同一路口重复计数。加入转弯和路口退出超时；仍未在硬件验证，双黑是否为路口独有信号也未证实。

## 验证状态

本次仅完成本地文件生成与语法层面的检查。没有完成目标 SDK 编译、测试程序执行、烧录或实际跑道测试，不能记录成“已经成功走完全程”。

## 下一次实测记录

| 项目 | 待填写 |
| --- | --- |
| 左、右白带实际宽度 | 按用户补充，各约 2.5 cm |
| 黑带 / 白带 / 地砖的左右输出 | 用户反馈归一化 black 值分别为 1 / 0 / 0；未独立复核原始 GPIO 电平 |
| 直行速度是否能平稳起步 | |
| 左黑时是否确实向左修正 | |
| 右黑时是否确实向右修正 | |
| 超声波读数与停车距离 | |
| 大 U 弯是否能在低速下通过 | |
| 两个岔路的传感器读数变化 | 用户反馈岔路口同时为 1,1；持续时间与转弯后的序列待测 |
| 是否允许临时覆盖短岔路入口 | |

下一步先做无岔路短段测试，再决定增加探头、标记或其他路口识别信息，不能把尚未验证的逻辑写成整条路线已实现。


