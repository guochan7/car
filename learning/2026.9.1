 工作日志：QST 小车桌面边缘避让系统

日期：2026-09-01  
项目：Hi3861 + STM32 双主控小车桌面边缘避让  
开发环境：OpenHarmony / Hi3861、Keil MDK 5 / STM32F103

## 一、今日目标

基于课程“系统通信协议”实验，完成一个由 Hi3861 和 STM32 协同控制的小车桌面边缘避让方案：

1. 小车正常向前行驶。
2. 左前红外检测到桌面边缘时，小车停止、短暂后退、向右转，然后继续前进。
3. 右前红外检测到桌面边缘时，小车停止、短暂后退、向左转，然后继续前进。
4. 两个红外同时检测到边缘时，小车停止、后退，并交替选择左转或右转。
5. Hi3861 负责传感器读取和行为决策，STM32 负责接收命令并驱动电机。

## 二、需求调整过程

最初计划让小车按照固定时间依次执行前进、后退、左转和右转。随后将需求调整为桌面边缘检测。

由于小车只有两个红外传感器，并且两个传感器都固定在车头，无法安全检测车尾边缘，因此最终确定以下策略：

```text
前进
→ 前端检测到边缘
→ 停止
→ 短暂后退离开边缘
→ 向远离边缘的方向转弯
→ 继续前进
```

该方案不改变传感器的现有安装位置。

## 三、最终系统架构

```text
左前、右前红外传感器
          ↓
       Hi3861
  消抖、边缘判断、状态机
          ↓ UART2
       STM32F103
  USART2接收、协议解析
  通信超时保护、PWM输出
          ↓
       左右电机
```

职责划分：

- Hi3861：读取 GPIO5、GPIO6，判断边缘方向，生成左右轮命令。
- STM32：通过 PA3/USART2_RX 接收命令，检查数据帧并控制左右电机 PWM。
- USART1：保留给 STM32 原工程打印和调试使用。

## 四、通信协议

采用固定 6 字节数据帧：

| 字节 | 内容 | 说明 |
| ---: | --- | --- |
| 0 | `0xFC` | 帧头 |
| 1 | 左轮方向 | `0` 正转，`1` 反转 |
| 2 | 左轮速度 | `0～150` |
| 3 | 右轮方向 | `0` 正转，`1` 反转 |
| 4 | 右轮速度 | `0～150` |
| 5 | `0xFD` | 帧尾 |

本项目使用的主要命令：

```text
前进：FC 00 3C 00 3C FD
停止：FC 00 00 00 00 FD
后退：FC 01 32 01 32 FD
左转：FC 01 32 00 32 FD
右转：FC 00 32 01 32 FD
```

## 五、Hi3861端完成内容

1. 配置 GPIO5、GPIO6 为两个前端红外传感器输入。
2. 配置 GPIO11、GPIO12 为 UART2 TX/RX。
3. 设置 UART 参数为 `115200 8N1`。
4. 对红外信号进行连续 3 次确认，降低传感器抖动造成的误判。
5. 使用非阻塞式状态机组织以下动作：
   - 正常前进；
   - 边缘停车；
   - 短暂后退；
   - 左转或右转；
   - 转弯后继续前进。
6. 每 20 ms 重复发送一次控制命令，降低单帧丢失造成的影响。
7. 整理 `BUILD.gn` 和上一级应用构建示例。

## 六、STM32端完成内容

1. 新增 USART2 通信模块：
   - PA2：USART2_TX；
   - PA3：USART2_RX。
2. 使用 `USART2_IRQHandler()` 接收和解析 6 字节数据帧。
3. 检查帧头、帧尾、方向和速度范围。
4. 将协议速度转换为左右电机 PWM。
5. 增加通信超时保护：连续 300 ms 未收到有效命令时自动停车。
6. 保留原工程 `usart.c` 和 `USART1_IRQHandler()`，避免修改课程原始调试串口代码。
7. 整理 `motor.c/.h`、`uart_protocol.c/.h`、`main.c` 和板级 API 声明。

## 七、编译问题与解决记录

### 1. 重复定义 `main()`

问题：把示例 `main()` 直接追加到原文件，导致同一文件存在两个入口函数。

解决：只保留一个 `main()`，将 USART 初始化和协议处理代码合并进原入口。

### 2. `delay_ms` 声明不一致

问题：原工程声明为：

```c
void delay_ms(u16 ms);
```

新代码错误地声明成：

```c
extern void delay_ms(u32 ms);
```

解决：删除重复声明，直接包含课程工程的 `delay.h`。

### 3. 找不到 `encoder.h`

问题：Keil 的 Include Paths 中没有编码器头文件目录。

处理方案：可以添加正确的头文件路径，或对原工程已有函数补充正确的函数原型。最终 GitHub 示例不依赖编码器头文件。

### 4. `USART1_IRQHandler` 重复定义

问题：原 `usart.c` 与新 `uart_protocol.c` 都定义了 `USART1_IRQHandler()`。

解决：不修改原 `usart.c`，将新通信链路整体迁移到 USART2：

```text
原USART1：打印和调试
新USART2：接收Hi3861电机命令
```

## 八、最终接线

### 红外传感器

```text
左前红外数字输出 → Hi3861 GPIO5
右前红外数字输出 → Hi3861 GPIO6
传感器GND        → Hi3861 GND
```

### 双主控串口

```text
Hi3861 GPIO11 / UART2_TX → STM32 PA3 / USART2_RX
Hi3861 GND               → STM32 GND
```

双向通信可选连接：

```text
Hi3861 GPIO12 / UART2_RX ← STM32 PA2 / USART2_TX
```

## 九、当前完成状态

- [x] 明确两个固定前端红外传感器的避边策略。
- [x] 完成 Hi3861 边缘判断和动作状态机代码。
- [x] 完成 6 字节 UART 命令打包。
- [x] 完成 STM32 USART2 接收和协议解析。
- [x] 完成 STM32 通信超时停车保护。
- [x] 避免 USART1 中断与原工程冲突。
- [x] 整理两个工程、README 和 GitHub `.gitignore`。
- [ ] 在实际 Keil 课程工程中完成最终版本复编。
- [ ] 分别烧录 Hi3861 和 STM32。
- [ ] 标定两个红外传感器的有效电平。
- [ ] 完成架空车轮测试。
- [ ] 完成地面低速测试。
- [ ] 完成有人保护的桌面测试。
