# 工作日志：嵌入式Linux学习与小车控制实践 (2026-08-25)

## 📌 今日总结
今天主要完成了虚拟机的环境搭建与基础学习，系统梳理了常用的 Linux 快捷命令，并重点实现了**代码的交叉编译、烧录到开发板**，最终成功让**小车做出了预期的动作响应**。

---

## 一、虚拟机学习笔记

### 1. 环境准备
*   **工具**：VMware Workstation / VirtualBox
*   **镜像**：Ubuntu 20.04 LTS（稳定版，对嵌入式开发友好）
*   **核心配置**：
    *   **网络模式**：选择了“桥接模式”或“NAT模式”，并配置了静态 IP，方便宿主机通过 SSH 连接。
    *   **共享文件夹**：配置了宿主机与虚拟机之间的共享文件夹，便于在 Windows 下编写代码，在 Linux 下编译。
    *   **快照功能**：在环境配置好之后立即打了一个快照，防止后续误操作导致系统崩溃重装。

### 2. 开发工具链安装
*   安装了 `vim` 编辑器以及 `gcc` / `arm-none-eabi-gcc` 交叉编译工具链。
*   学会了使用 `MobaXterm` 或 `Xshell` 通过 SSH 登录虚拟机，提高敲代码的效率。

---

## 二、Linux 常用快捷命令整理（今日掌握）

| 类别 | 命令 | 作用说明 |
| :--- | :--- | :--- |
| **文件操作** | `ls` / `ls -al` | 查看当前目录文件，以及隐藏文件的详细信息 |
| | `cd ..` / `cd /home` | 返回上一级目录 / 进入指定目录 |
| | `mkdir test` | 创建新目录 |
| | `rm -rf test` | **警告**：强制递归删除文件/目录 |
| | `cp a.txt b.txt` | 复制文件 |
| | `mv a.txt b.txt` | 移动或重命名文件 |
| **查看编辑** | `cat main.c` | 查看文件内容 |
| | `vim main.c` | 编辑文件（按 `i` 进入插入模式，按 `Esc` 后输入 `:wq` 保存退出） |
| **权限管理** | `chmod +x script.sh` | 给脚本增加可执行权限 |
| | `chmod 777 file` | 设置最高权限 |
| **系统与网络** | `df -h` | 查看磁盘空间使用情况 |
| | `free -h` | 查看内存使用情况 |
| | `top` | 查看系统进程和负载 |
| | `ping 192.168.1.1` | 测试网络连通性 |
| | `ifconfig` | 查看网络接口信息 |
| | `lsusb` | **重点**：查看 USB 设备（常用于确认 ST-Link 是否已连接） |

---

## 三、代码交叉编译与小车烧录反应流程（核心实操）

今天将编写的控制代码（结合了之前的炫彩灯逻辑与电机控制逻辑）成功交叉编译并烧录到小车主控板上。

### 1. 硬件连接（接线明细）
*   **下载器连接**：使用 ST-Link 下载器，将 SWDIO 接 PA13，SWCLK 接 PA14，GND 接 GND，3.3V 接 3.3V。
*   **电机驱动模块连接**：小车电机驱动模块（如 TB6612/L298N）接入主控板。IN1、IN2 控制左电机；IN3、IN4 控制右电机；PWM 引脚接定时器输出通道（如 PA0、PA1）以控制速度，STBY 引脚接 5V 使能。
*   **彩色灯带（WS2812）连接**：根据原代码定义，**前灯 DIL 接 GPIOC Pin 13**，**后灯 DIR 接 GPIOC Pin 14**，VCC 接 5V，GND 接 GND。
*   **供电**：使用 7.4V 锂电池为电机驱动和主控板供电。

### 2. 交叉编译
在虚拟机的终端下，使用 Makefile 或直接调用交叉编译工具：
```bash
# 使用 ARM 交叉编译工具链进行编译
arm-none-eabi-gcc -mcpu=cortex-m3 -mthumb -c main.c -o main.o
arm-none-eabi-gcc -mcpu=cortex-m3 -mthumb main.o -T stm32f103.ld -o main.elf

# 生成 bin 或 hex 文件供烧录使用
arm-none-eabi-objcopy -O binary main.elf main.bin
